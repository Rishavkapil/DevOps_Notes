AWS Global Accelerator is a Networking service that improves availability & performance of your application with global users. 

It does by : 
- Giving your app two static anycast IP addresses. 
- Routing user traffic through the AWS global Network 
- Sending traffic to the closest healthy AWS region where your app is running. 