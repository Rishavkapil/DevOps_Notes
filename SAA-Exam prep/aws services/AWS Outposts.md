AWS inside your own data center.
AWS outposts is a service that lets you run AWS Infra & AWS Services on-premise, while still using AWS cloud. 

e.g I want EC2 , EBS , RDS... but physically inside my building. 

AWS ships you a rack of servers, installs it in your data center, connects it to an AWS region, & you manage it using same AWS console. 