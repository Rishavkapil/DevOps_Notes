For having transitive peering between thousands of VPC and on-premise , hub-and-spoke connection. 

We can connect multiple VPC with each other using transitive gateway, 

we just need to add a peering connection for each vpc with transitive gateway. 

we can also add a peering connection with aws direct connect . 